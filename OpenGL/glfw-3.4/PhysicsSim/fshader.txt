#version 460 core

in vec3 ourColor;
in vec4 lightNorm;
in vec4 lightDir;

out vec4 FragColor;

float ambient = 0.5f;

void main()
{
	float diffuseVal = max(dot(lightDir, lightNorm), 0.0f);
	FragColor = vec4 (ourColor * (diffuseVal + ambient), 1.0);
}